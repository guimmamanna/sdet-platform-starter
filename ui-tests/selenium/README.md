# Selenium

Selenium is included to show pragmatic support for legacy or mixed-stack environments.

- Python-based page objects
- explicit waits instead of fixed sleeps
- reusable browser factory with Chrome or Firefox support
- deterministic state reset through the sample API

Run it with:

```bash
pytest ui-tests/selenium/tests -m smoke
```

